import "../stylesheets/ApiError.scss";

const ApiError = () => {
  return (
    <p className="main__apiError">
      Lo sentimos, no se ha podido cargar la lista de personajes. Le rogamos lo
      intente de nuevo más tarde.
    </p>
  );
};

export default ApiError;
